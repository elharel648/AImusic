# The single source of truth for dependencies is the repo-root requirements.txt
# (this file once diverged from it — never again). Install from the root:
#   .venv/bin/pip install -r requirements.txt
-r ../requirements.txt
